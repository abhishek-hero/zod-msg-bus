{
  "name": "zod-msg-bus",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "vitest",
    "test": "vitest run",
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "lint": "tsc",
    "ci": "yarn run lint && yarn run build"
  },
  "author": "abhishek-hero",
  "dependencies": {
    "@types/node": "^20.4.8",
    "tsup": "^7.2.0",
    "typescript": "^5.1.6"
  }
}
